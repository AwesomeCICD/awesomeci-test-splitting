// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 3;
const FACTOR_BASE = 131;

function paymentRetry(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function paymentRetryWithFee(amount, factor, fee) {
  return paymentRetry(amount, factor) + fee;
}

function clampPaymentRetry(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  paymentRetry,
  paymentRetryWithFee,
  clampPaymentRetry,
  factorFromBps,
};
