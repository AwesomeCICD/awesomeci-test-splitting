// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 118;

function captureDelay(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function captureDelayWithFee(amount, factor, fee) {
  return captureDelay(amount, factor) + fee;
}

function clampCaptureDelay(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  captureDelay,
  captureDelayWithFee,
  clampCaptureDelay,
  factorFromBps,
};
