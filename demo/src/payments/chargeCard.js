// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 137;

function chargeCard(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function chargeCardWithFee(amount, factor, fee) {
  return chargeCard(amount, factor) + fee;
}

function clampChargeCard(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  chargeCard,
  chargeCardWithFee,
  clampChargeCard,
  factorFromBps,
};
