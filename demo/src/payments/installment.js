// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 3;
const FACTOR_BASE = 102;

function installment(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function installmentWithFee(amount, factor, fee) {
  return installment(amount, factor) + fee;
}

function clampInstallment(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  installment,
  installmentWithFee,
  clampInstallment,
  factorFromBps,
};
