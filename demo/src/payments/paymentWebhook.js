// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 108;

function paymentWebhook(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function paymentWebhookWithFee(amount, factor, fee) {
  return paymentWebhook(amount, factor) + fee;
}

function clampPaymentWebhook(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  paymentWebhook,
  paymentWebhookWithFee,
  clampPaymentWebhook,
  factorFromBps,
};
