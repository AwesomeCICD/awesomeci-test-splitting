// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 116;

function walletPay(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function walletPayWithFee(amount, factor, fee) {
  return walletPay(amount, factor) + fee;
}

function clampWalletPay(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  walletPay,
  walletPayWithFee,
  clampWalletPay,
  factorFromBps,
};
