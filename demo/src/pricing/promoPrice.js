// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 126;

function promoPrice(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function promoPriceWithFee(amount, factor, fee) {
  return promoPrice(amount, factor) + fee;
}

function clampPromoPrice(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  promoPrice,
  promoPriceWithFee,
  clampPromoPrice,
  factorFromBps,
};
