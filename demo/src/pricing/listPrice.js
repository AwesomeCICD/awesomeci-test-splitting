// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 135;

function listPrice(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function listPriceWithFee(amount, factor, fee) {
  return listPrice(amount, factor) + fee;
}

function clampListPrice(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  listPrice,
  listPriceWithFee,
  clampListPrice,
  factorFromBps,
};
