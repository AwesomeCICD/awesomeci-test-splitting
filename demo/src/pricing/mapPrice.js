// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 3;
const FACTOR_BASE = 121;

function mapPrice(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function mapPriceWithFee(amount, factor, fee) {
  return mapPrice(amount, factor) + fee;
}

function clampMapPrice(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  mapPrice,
  mapPriceWithFee,
  clampMapPrice,
  factorFromBps,
};
