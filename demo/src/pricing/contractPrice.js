// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 109;

function contractPrice(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function contractPriceWithFee(amount, factor, fee) {
  return contractPrice(amount, factor) + fee;
}

function clampContractPrice(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  contractPrice,
  contractPriceWithFee,
  clampContractPrice,
  factorFromBps,
};
