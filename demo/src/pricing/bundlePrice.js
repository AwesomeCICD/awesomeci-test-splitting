// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 131;

function bundlePrice(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function bundlePriceWithFee(amount, factor, fee) {
  return bundlePrice(amount, factor) + fee;
}

function clampBundlePrice(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  bundlePrice,
  bundlePriceWithFee,
  clampBundlePrice,
  factorFromBps,
};
