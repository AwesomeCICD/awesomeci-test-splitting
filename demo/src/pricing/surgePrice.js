// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 109;

function surgePrice(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function surgePriceWithFee(amount, factor, fee) {
  return surgePrice(amount, factor) + fee;
}

function clampSurgePrice(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  surgePrice,
  surgePriceWithFee,
  clampSurgePrice,
  factorFromBps,
};
