// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 4;
const FACTOR_BASE = 100;

function clearance(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function clearanceWithFee(amount, factor, fee) {
  return clearance(amount, factor) + fee;
}

function clampClearance(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  clearance,
  clearanceWithFee,
  clampClearance,
  factorFromBps,
};
