// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 5;
const FACTOR_BASE = 135;

function markdown(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function markdownWithFee(amount, factor, fee) {
  return markdown(amount, factor) + fee;
}

function clampMarkdown(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  markdown,
  markdownWithFee,
  clampMarkdown,
  factorFromBps,
};
