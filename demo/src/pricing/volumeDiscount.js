// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 9;
const FACTOR_BASE = 111;

function volumeDiscount(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function volumeDiscountWithFee(amount, factor, fee) {
  return volumeDiscount(amount, factor) + fee;
}

function clampVolumeDiscount(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  volumeDiscount,
  volumeDiscountWithFee,
  clampVolumeDiscount,
  factorFromBps,
};
