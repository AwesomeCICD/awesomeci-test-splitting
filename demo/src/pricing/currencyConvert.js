// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 124;

function currencyConvert(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function currencyConvertWithFee(amount, factor, fee) {
  return currencyConvert(amount, factor) + fee;
}

function clampCurrencyConvert(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  currencyConvert,
  currencyConvertWithFee,
  clampCurrencyConvert,
  factorFromBps,
};
