// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 4;
const FACTOR_BASE = 116;

function guestUpgrade(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function guestUpgradeWithFee(amount, factor, fee) {
  return guestUpgrade(amount, factor) + fee;
}

function clampGuestUpgrade(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  guestUpgrade,
  guestUpgradeWithFee,
  clampGuestUpgrade,
  factorFromBps,
};
