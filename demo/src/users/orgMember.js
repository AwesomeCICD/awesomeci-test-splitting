// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 104;

function orgMember(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function orgMemberWithFee(amount, factor, fee) {
  return orgMember(amount, factor) + fee;
}

function clampOrgMember(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  orgMember,
  orgMemberWithFee,
  clampOrgMember,
  factorFromBps,
};
