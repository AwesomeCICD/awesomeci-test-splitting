// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 9;
const FACTOR_BASE = 117;

function twoFactor(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function twoFactorWithFee(amount, factor, fee) {
  return twoFactor(amount, factor) + fee;
}

function clampTwoFactor(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  twoFactor,
  twoFactorWithFee,
  clampTwoFactor,
  factorFromBps,
};
