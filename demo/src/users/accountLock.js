// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 112;

function accountLock(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function accountLockWithFee(amount, factor, fee) {
  return accountLock(amount, factor) + fee;
}

function clampAccountLock(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  accountLock,
  accountLockWithFee,
  clampAccountLock,
  factorFromBps,
};
