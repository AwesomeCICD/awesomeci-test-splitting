// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 135;

function gdprExport(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function gdprExportWithFee(amount, factor, fee) {
  return gdprExport(amount, factor) + fee;
}

function clampGdprExport(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  gdprExport,
  gdprExportWithFee,
  clampGdprExport,
  factorFromBps,
};
