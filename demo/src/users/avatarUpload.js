// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 136;

function avatarUpload(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function avatarUploadWithFee(amount, factor, fee) {
  return avatarUpload(amount, factor) + fee;
}

function clampAvatarUpload(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  avatarUpload,
  avatarUploadWithFee,
  clampAvatarUpload,
  factorFromBps,
};
