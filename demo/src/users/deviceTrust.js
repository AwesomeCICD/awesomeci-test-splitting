// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 120;

function deviceTrust(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function deviceTrustWithFee(amount, factor, fee) {
  return deviceTrust(amount, factor) + fee;
}

function clampDeviceTrust(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  deviceTrust,
  deviceTrustWithFee,
  clampDeviceTrust,
  factorFromBps,
};
