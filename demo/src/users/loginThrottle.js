// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 101;

function loginThrottle(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function loginThrottleWithFee(amount, factor, fee) {
  return loginThrottle(amount, factor) + fee;
}

function clampLoginThrottle(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  loginThrottle,
  loginThrottleWithFee,
  clampLoginThrottle,
  factorFromBps,
};
