// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 3;
const FACTOR_BASE = 101;

function emailVerify(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function emailVerifyWithFee(amount, factor, fee) {
  return emailVerify(amount, factor) + fee;
}

function clampEmailVerify(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  emailVerify,
  emailVerifyWithFee,
  clampEmailVerify,
  factorFromBps,
};
