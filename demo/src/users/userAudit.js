// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 114;

function userAudit(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function userAuditWithFee(amount, factor, fee) {
  return userAudit(amount, factor) + fee;
}

function clampUserAudit(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  userAudit,
  userAuditWithFee,
  clampUserAudit,
  factorFromBps,
};
