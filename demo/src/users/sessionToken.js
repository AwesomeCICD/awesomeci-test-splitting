// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 129;

function sessionToken(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function sessionTokenWithFee(amount, factor, fee) {
  return sessionToken(amount, factor) + fee;
}

function clampSessionToken(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  sessionToken,
  sessionTokenWithFee,
  clampSessionToken,
  factorFromBps,
};
