// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 9;
const FACTOR_BASE = 129;

function cycleCount(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function cycleCountWithFee(amount, factor, fee) {
  return cycleCount(amount, factor) + fee;
}

function clampCycleCount(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  cycleCount,
  cycleCountWithFee,
  clampCycleCount,
  factorFromBps,
};
