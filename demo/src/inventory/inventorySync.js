// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 139;

function inventorySync(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function inventorySyncWithFee(amount, factor, fee) {
  return inventorySync(amount, factor) + fee;
}

function clampInventorySync(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  inventorySync,
  inventorySyncWithFee,
  clampInventorySync,
  factorFromBps,
};
