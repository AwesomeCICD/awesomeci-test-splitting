// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 3;
const FACTOR_BASE = 123;

function reserveUnits(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function reserveUnitsWithFee(amount, factor, fee) {
  return reserveUnits(amount, factor) + fee;
}

function clampReserveUnits(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  reserveUnits,
  reserveUnitsWithFee,
  clampReserveUnits,
  factorFromBps,
};
