// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 108;

function releaseHold(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function releaseHoldWithFee(amount, factor, fee) {
  return releaseHold(amount, factor) + fee;
}

function clampReleaseHold(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  releaseHold,
  releaseHoldWithFee,
  clampReleaseHold,
  factorFromBps,
};
