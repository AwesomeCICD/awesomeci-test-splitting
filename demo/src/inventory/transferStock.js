// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 4;
const FACTOR_BASE = 123;

function transferStock(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function transferStockWithFee(amount, factor, fee) {
  return transferStock(amount, factor) + fee;
}

function clampTransferStock(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  transferStock,
  transferStockWithFee,
  clampTransferStock,
  factorFromBps,
};
