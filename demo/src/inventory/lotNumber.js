// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 114;

function lotNumber(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function lotNumberWithFee(amount, factor, fee) {
  return lotNumber(amount, factor) + fee;
}

function clampLotNumber(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  lotNumber,
  lotNumberWithFee,
  clampLotNumber,
  factorFromBps,
};
