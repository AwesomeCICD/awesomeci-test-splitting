// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 124;

function sellableQty(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function sellableQtyWithFee(amount, factor, fee) {
  return sellableQty(amount, factor) + fee;
}

function clampSellableQty(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  sellableQty,
  sellableQtyWithFee,
  clampSellableQty,
  factorFromBps,
};
