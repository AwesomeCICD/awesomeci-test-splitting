// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 134;

function shrinkAdjust(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function shrinkAdjustWithFee(amount, factor, fee) {
  return shrinkAdjust(amount, factor) + fee;
}

function clampShrinkAdjust(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  shrinkAdjust,
  shrinkAdjustWithFee,
  clampShrinkAdjust,
  factorFromBps,
};
