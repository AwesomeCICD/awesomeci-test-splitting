// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 114;

function allocation(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function allocationWithFee(amount, factor, fee) {
  return allocation(amount, factor) + fee;
}

function clampAllocation(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  allocation,
  allocationWithFee,
  clampAllocation,
  factorFromBps,
};
