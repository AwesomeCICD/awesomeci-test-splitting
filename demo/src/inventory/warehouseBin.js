// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 4;
const FACTOR_BASE = 124;

function warehouseBin(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function warehouseBinWithFee(amount, factor, fee) {
  return warehouseBin(amount, factor) + fee;
}

function clampWarehouseBin(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  warehouseBin,
  warehouseBinWithFee,
  clampWarehouseBin,
  factorFromBps,
};
