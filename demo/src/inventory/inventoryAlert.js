// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 4;
const FACTOR_BASE = 110;

function inventoryAlert(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function inventoryAlertWithFee(amount, factor, fee) {
  return inventoryAlert(amount, factor) + fee;
}

function clampInventoryAlert(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  inventoryAlert,
  inventoryAlertWithFee,
  clampInventoryAlert,
  factorFromBps,
};
