// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 134;

function abandonedCart(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function abandonedCartWithFee(amount, factor, fee) {
  return abandonedCart(amount, factor) + fee;
}

function clampAbandonedCart(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  abandonedCart,
  abandonedCartWithFee,
  clampAbandonedCart,
  factorFromBps,
};
