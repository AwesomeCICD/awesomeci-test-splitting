// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 135;

function cartNotes(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function cartNotesWithFee(amount, factor, fee) {
  return cartNotes(amount, factor) + fee;
}

function clampCartNotes(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  cartNotes,
  cartNotesWithFee,
  clampCartNotes,
  factorFromBps,
};
