// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 129;

function cartCurrency(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function cartCurrencyWithFee(amount, factor, fee) {
  return cartCurrency(amount, factor) + fee;
}

function clampCartCurrency(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  cartCurrency,
  cartCurrencyWithFee,
  clampCartCurrency,
  factorFromBps,
};
