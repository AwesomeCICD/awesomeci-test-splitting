// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 112;

function addItem(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function addItemWithFee(amount, factor, fee) {
  return addItem(amount, factor) + fee;
}

function clampAddItem(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  addItem,
  addItemWithFee,
  clampAddItem,
  factorFromBps,
};
