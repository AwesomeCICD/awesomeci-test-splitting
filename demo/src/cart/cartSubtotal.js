// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 3;
const FACTOR_BASE = 130;

function cartSubtotal(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function cartSubtotalWithFee(amount, factor, fee) {
  return cartSubtotal(amount, factor) + fee;
}

function clampCartSubtotal(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  cartSubtotal,
  cartSubtotalWithFee,
  clampCartSubtotal,
  factorFromBps,
};
