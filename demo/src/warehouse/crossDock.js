// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 124;

function crossDock(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function crossDockWithFee(amount, factor, fee) {
  return crossDock(amount, factor) + fee;
}

function clampCrossDock(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  crossDock,
  crossDockWithFee,
  clampCrossDock,
  factorFromBps,
};
