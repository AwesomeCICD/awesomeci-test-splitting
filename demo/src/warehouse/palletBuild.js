// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 5;
const FACTOR_BASE = 137;

function palletBuild(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function palletBuildWithFee(amount, factor, fee) {
  return palletBuild(amount, factor) + fee;
}

function clampPalletBuild(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  palletBuild,
  palletBuildWithFee,
  clampPalletBuild,
  factorFromBps,
};
