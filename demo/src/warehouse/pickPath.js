// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 135;

function pickPath(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function pickPathWithFee(amount, factor, fee) {
  return pickPath(amount, factor) + fee;
}

function clampPickPath(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  pickPath,
  pickPathWithFee,
  clampPickPath,
  factorFromBps,
};
