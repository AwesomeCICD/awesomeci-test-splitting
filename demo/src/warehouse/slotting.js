// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 105;

function slotting(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function slottingWithFee(amount, factor, fee) {
  return slotting(amount, factor) + fee;
}

function clampSlotting(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  slotting,
  slottingWithFee,
  clampSlotting,
  factorFromBps,
};
