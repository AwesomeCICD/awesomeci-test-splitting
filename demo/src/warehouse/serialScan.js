// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 5;
const FACTOR_BASE = 132;

function serialScan(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function serialScanWithFee(amount, factor, fee) {
  return serialScan(amount, factor) + fee;
}

function clampSerialScan(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  serialScan,
  serialScanWithFee,
  clampSerialScan,
  factorFromBps,
};
