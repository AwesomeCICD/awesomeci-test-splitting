// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 9;
const FACTOR_BASE = 133;

function packVerify(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function packVerifyWithFee(amount, factor, fee) {
  return packVerify(amount, factor) + fee;
}

function clampPackVerify(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  packVerify,
  packVerifyWithFee,
  clampPackVerify,
  factorFromBps,
};
