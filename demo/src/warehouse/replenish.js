// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 113;

function replenish(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function replenishWithFee(amount, factor, fee) {
  return replenish(amount, factor) + fee;
}

function clampReplenish(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  replenish,
  replenishWithFee,
  clampReplenish,
  factorFromBps,
};
