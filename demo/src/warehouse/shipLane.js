// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 125;

function shipLane(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function shipLaneWithFee(amount, factor, fee) {
  return shipLane(amount, factor) + fee;
}

function clampShipLane(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  shipLane,
  shipLaneWithFee,
  clampShipLane,
  factorFromBps,
};
