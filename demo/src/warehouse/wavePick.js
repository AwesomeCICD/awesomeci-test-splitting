// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 109;

function wavePick(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function wavePickWithFee(amount, factor, fee) {
  return wavePick(amount, factor) + fee;
}

function clampWavePick(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  wavePick,
  wavePickWithFee,
  clampWavePick,
  factorFromBps,
};
