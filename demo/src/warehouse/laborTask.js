// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 112;

function laborTask(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function laborTaskWithFee(amount, factor, fee) {
  return laborTask(amount, factor) + fee;
}

function clampLaborTask(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  laborTask,
  laborTaskWithFee,
  clampLaborTask,
  factorFromBps,
};
