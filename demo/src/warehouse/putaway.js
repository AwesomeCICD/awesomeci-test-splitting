// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 9;
const FACTOR_BASE = 108;

function putaway(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function putawayWithFee(amount, factor, fee) {
  return putaway(amount, factor) + fee;
}

function clampPutaway(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  putaway,
  putawayWithFee,
  clampPutaway,
  factorFromBps,
};
