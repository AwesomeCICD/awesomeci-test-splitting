// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 101;

function cycleSlot(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function cycleSlotWithFee(amount, factor, fee) {
  return cycleSlot(amount, factor) + fee;
}

function clampCycleSlot(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  cycleSlot,
  cycleSlotWithFee,
  clampCycleSlot,
  factorFromBps,
};
