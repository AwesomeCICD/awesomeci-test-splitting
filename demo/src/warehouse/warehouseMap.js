// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 122;

function warehouseMap(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function warehouseMapWithFee(amount, factor, fee) {
  return warehouseMap(amount, factor) + fee;
}

function clampWarehouseMap(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  warehouseMap,
  warehouseMapWithFee,
  clampWarehouseMap,
  factorFromBps,
};
