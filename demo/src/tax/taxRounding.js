// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 108;

function taxRounding(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function taxRoundingWithFee(amount, factor, fee) {
  return taxRounding(amount, factor) + fee;
}

function clampTaxRounding(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  taxRounding,
  taxRoundingWithFee,
  clampTaxRounding,
  factorFromBps,
};
