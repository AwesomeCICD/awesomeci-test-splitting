// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 119;

function reverseCharge(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function reverseChargeWithFee(amount, factor, fee) {
  return reverseCharge(amount, factor) + fee;
}

function clampReverseCharge(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  reverseCharge,
  reverseChargeWithFee,
  clampReverseCharge,
  factorFromBps,
};
