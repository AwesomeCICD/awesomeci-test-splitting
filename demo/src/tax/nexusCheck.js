// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 114;

function nexusCheck(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function nexusCheckWithFee(amount, factor, fee) {
  return nexusCheck(amount, factor) + fee;
}

function clampNexusCheck(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  nexusCheck,
  nexusCheckWithFee,
  clampNexusCheck,
  factorFromBps,
};
