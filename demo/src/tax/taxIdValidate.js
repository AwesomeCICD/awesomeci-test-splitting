// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 5;
const FACTOR_BASE = 103;

function taxIdValidate(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function taxIdValidateWithFee(amount, factor, fee) {
  return taxIdValidate(amount, factor) + fee;
}

function clampTaxIdValidate(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  taxIdValidate,
  taxIdValidateWithFee,
  clampTaxIdValidate,
  factorFromBps,
};
