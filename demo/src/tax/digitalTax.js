// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 110;

function digitalTax(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function digitalTaxWithFee(amount, factor, fee) {
  return digitalTax(amount, factor) + fee;
}

function clampDigitalTax(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  digitalTax,
  digitalTaxWithFee,
  clampDigitalTax,
  factorFromBps,
};
