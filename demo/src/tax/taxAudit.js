// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 107;

function taxAudit(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function taxAuditWithFee(amount, factor, fee) {
  return taxAudit(amount, factor) + fee;
}

function clampTaxAudit(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  taxAudit,
  taxAuditWithFee,
  clampTaxAudit,
  factorFromBps,
};
