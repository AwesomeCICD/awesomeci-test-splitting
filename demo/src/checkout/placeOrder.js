// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 115;

function placeOrder(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function placeOrderWithFee(amount, factor, fee) {
  return placeOrder(amount, factor) + fee;
}

function clampPlaceOrder(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  placeOrder,
  placeOrderWithFee,
  clampPlaceOrder,
  factorFromBps,
};
