// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 115;

function checkoutHold(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function checkoutHoldWithFee(amount, factor, fee) {
  return checkoutHold(amount, factor) + fee;
}

function clampCheckoutHold(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  checkoutHold,
  checkoutHoldWithFee,
  clampCheckoutHold,
  factorFromBps,
};
