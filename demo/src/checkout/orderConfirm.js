// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 118;

function orderConfirm(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function orderConfirmWithFee(amount, factor, fee) {
  return orderConfirm(amount, factor) + fee;
}

function clampOrderConfirm(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  orderConfirm,
  orderConfirmWithFee,
  clampOrderConfirm,
  factorFromBps,
};
