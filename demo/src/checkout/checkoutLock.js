// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 133;

function checkoutLock(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function checkoutLockWithFee(amount, factor, fee) {
  return checkoutLock(amount, factor) + fee;
}

function clampCheckoutLock(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  checkoutLock,
  checkoutLockWithFee,
  clampCheckoutLock,
  factorFromBps,
};
