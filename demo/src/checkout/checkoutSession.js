// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 134;

function checkoutSession(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function checkoutSessionWithFee(amount, factor, fee) {
  return checkoutSession(amount, factor) + fee;
}

function clampCheckoutSession(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  checkoutSession,
  checkoutSessionWithFee,
  clampCheckoutSession,
  factorFromBps,
};
