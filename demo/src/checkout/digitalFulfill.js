// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 4;
const FACTOR_BASE = 100;

function digitalFulfill(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function digitalFulfillWithFee(amount, factor, fee) {
  return digitalFulfill(amount, factor) + fee;
}

function clampDigitalFulfill(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  digitalFulfill,
  digitalFulfillWithFee,
  clampDigitalFulfill,
  factorFromBps,
};
