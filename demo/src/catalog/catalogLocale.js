// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 4;
const FACTOR_BASE = 126;

function catalogLocale(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function catalogLocaleWithFee(amount, factor, fee) {
  return catalogLocale(amount, factor) + fee;
}

function clampCatalogLocale(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  catalogLocale,
  catalogLocaleWithFee,
  clampCatalogLocale,
  factorFromBps,
};
