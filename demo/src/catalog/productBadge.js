// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 5;
const FACTOR_BASE = 107;

function productBadge(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function productBadgeWithFee(amount, factor, fee) {
  return productBadge(amount, factor) + fee;
}

function clampProductBadge(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  productBadge,
  productBadgeWithFee,
  clampProductBadge,
  factorFromBps,
};
