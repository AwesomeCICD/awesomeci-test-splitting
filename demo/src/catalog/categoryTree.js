// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 125;

function categoryTree(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function categoryTreeWithFee(amount, factor, fee) {
  return categoryTree(amount, factor) + fee;
}

function clampCategoryTree(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  categoryTree,
  categoryTreeWithFee,
  clampCategoryTree,
  factorFromBps,
};
