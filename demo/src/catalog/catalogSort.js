// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 4;
const FACTOR_BASE = 120;

function catalogSort(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function catalogSortWithFee(amount, factor, fee) {
  return catalogSort(amount, factor) + fee;
}

function clampCatalogSort(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  catalogSort,
  catalogSortWithFee,
  clampCatalogSort,
  factorFromBps,
};
