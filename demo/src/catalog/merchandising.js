// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 119;

function merchandising(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function merchandisingWithFee(amount, factor, fee) {
  return merchandising(amount, factor) + fee;
}

function clampMerchandising(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  merchandising,
  merchandisingWithFee,
  clampMerchandising,
  factorFromBps,
};
