// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 115;

function searchIndex(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function searchIndexWithFee(amount, factor, fee) {
  return searchIndex(amount, factor) + fee;
}

function clampSearchIndex(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  searchIndex,
  searchIndexWithFee,
  clampSearchIndex,
  factorFromBps,
};
