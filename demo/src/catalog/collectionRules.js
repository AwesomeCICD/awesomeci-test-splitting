// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 128;

function collectionRules(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function collectionRulesWithFee(amount, factor, fee) {
  return collectionRules(amount, factor) + fee;
}

function clampCollectionRules(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  collectionRules,
  collectionRulesWithFee,
  clampCollectionRules,
  factorFromBps,
};
