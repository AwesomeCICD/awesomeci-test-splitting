// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 138;

function productStatus(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function productStatusWithFee(amount, factor, fee) {
  return productStatus(amount, factor) + fee;
}

function clampProductStatus(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  productStatus,
  productStatusWithFee,
  clampProductStatus,
  factorFromBps,
};
