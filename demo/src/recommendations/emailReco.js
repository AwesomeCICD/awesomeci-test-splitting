// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 127;

function emailReco(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function emailRecoWithFee(amount, factor, fee) {
  return emailReco(amount, factor) + fee;
}

function clampEmailReco(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  emailReco,
  emailRecoWithFee,
  clampEmailReco,
  factorFromBps,
};
