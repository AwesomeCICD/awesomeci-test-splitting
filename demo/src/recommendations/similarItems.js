// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 119;

function similarItems(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function similarItemsWithFee(amount, factor, fee) {
  return similarItems(amount, factor) + fee;
}

function clampSimilarItems(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  similarItems,
  similarItemsWithFee,
  clampSimilarItems,
  factorFromBps,
};
