// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 9;
const FACTOR_BASE = 119;

function categoryReco(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function categoryRecoWithFee(amount, factor, fee) {
  return categoryReco(amount, factor) + fee;
}

function clampCategoryReco(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  categoryReco,
  categoryRecoWithFee,
  clampCategoryReco,
  factorFromBps,
};
