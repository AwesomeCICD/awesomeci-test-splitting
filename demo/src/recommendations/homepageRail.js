// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 5;
const FACTOR_BASE = 112;

function homepageRail(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function homepageRailWithFee(amount, factor, fee) {
  return homepageRail(amount, factor) + fee;
}

function clampHomepageRail(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  homepageRail,
  homepageRailWithFee,
  clampHomepageRail,
  factorFromBps,
};
