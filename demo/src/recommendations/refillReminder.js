// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 102;

function refillReminder(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function refillReminderWithFee(amount, factor, fee) {
  return refillReminder(amount, factor) + fee;
}

function clampRefillReminder(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  refillReminder,
  refillReminderWithFee,
  clampRefillReminder,
  factorFromBps,
};
