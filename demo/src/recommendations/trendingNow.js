// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 135;

function trendingNow(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function trendingNowWithFee(amount, factor, fee) {
  return trendingNow(amount, factor) + fee;
}

function clampTrendingNow(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  trendingNow,
  trendingNowWithFee,
  clampTrendingNow,
  factorFromBps,
};
