// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 121;

function cartReco(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function cartRecoWithFee(amount, factor, fee) {
  return cartReco(amount, factor) + fee;
}

function clampCartReco(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  cartReco,
  cartRecoWithFee,
  clampCartReco,
  factorFromBps,
};
