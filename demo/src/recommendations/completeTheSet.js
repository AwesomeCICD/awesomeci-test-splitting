// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 134;

function completeTheSet(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function completeTheSetWithFee(amount, factor, fee) {
  return completeTheSet(amount, factor) + fee;
}

function clampCompleteTheSet(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  completeTheSet,
  completeTheSetWithFee,
  clampCompleteTheSet,
  factorFromBps,
};
