// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 110;

function coldStart(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function coldStartWithFee(amount, factor, fee) {
  return coldStart(amount, factor) + fee;
}

function clampColdStart(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  coldStart,
  coldStartWithFee,
  clampColdStart,
  factorFromBps,
};
