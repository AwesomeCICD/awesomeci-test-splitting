// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 3;
const FACTOR_BASE = 119;

function becauseYouViewed(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function becauseYouViewedWithFee(amount, factor, fee) {
  return becauseYouViewed(amount, factor) + fee;
}

function clampBecauseYouViewed(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  becauseYouViewed,
  becauseYouViewedWithFee,
  clampBecauseYouViewed,
  factorFromBps,
};
