// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 110;

function ruralSurcharge(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function ruralSurchargeWithFee(amount, factor, fee) {
  return ruralSurcharge(amount, factor) + fee;
}

function clampRuralSurcharge(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  ruralSurcharge,
  ruralSurchargeWithFee,
  clampRuralSurcharge,
  factorFromBps,
};
