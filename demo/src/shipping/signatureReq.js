// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 122;

function signatureReq(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function signatureReqWithFee(amount, factor, fee) {
  return signatureReq(amount, factor) + fee;
}

function clampSignatureReq(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  signatureReq,
  signatureReqWithFee,
  clampSignatureReq,
  factorFromBps,
};
