// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 5;
const FACTOR_BASE = 125;

function carrierPick(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function carrierPickWithFee(amount, factor, fee) {
  return carrierPick(amount, factor) + fee;
}

function clampCarrierPick(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  carrierPick,
  carrierPickWithFee,
  clampCarrierPick,
  factorFromBps,
};
