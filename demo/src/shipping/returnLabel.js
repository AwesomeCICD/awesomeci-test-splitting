// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 124;

function returnLabel(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function returnLabelWithFee(amount, factor, fee) {
  return returnLabel(amount, factor) + fee;
}

function clampReturnLabel(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  returnLabel,
  returnLabelWithFee,
  clampReturnLabel,
  factorFromBps,
};
