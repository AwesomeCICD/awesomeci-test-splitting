// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 5;
const FACTOR_BASE = 122;

function expedite(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function expediteWithFee(amount, factor, fee) {
  return expedite(amount, factor) + fee;
}

function clampExpedite(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  expedite,
  expediteWithFee,
  clampExpedite,
  factorFromBps,
};
