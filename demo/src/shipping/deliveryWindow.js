// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 132;

function deliveryWindow(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function deliveryWindowWithFee(amount, factor, fee) {
  return deliveryWindow(amount, factor) + fee;
}

function clampDeliveryWindow(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  deliveryWindow,
  deliveryWindowWithFee,
  clampDeliveryWindow,
  factorFromBps,
};
