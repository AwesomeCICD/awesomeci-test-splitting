// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 3;
const FACTOR_BASE = 105;

function boxRecommend(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function boxRecommendWithFee(amount, factor, fee) {
  return boxRecommend(amount, factor) + fee;
}

function clampBoxRecommend(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  boxRecommend,
  boxRecommendWithFee,
  clampBoxRecommend,
  factorFromBps,
};
