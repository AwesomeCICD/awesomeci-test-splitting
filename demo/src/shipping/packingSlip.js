// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 7;
const FACTOR_BASE = 105;

function packingSlip(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function packingSlipWithFee(amount, factor, fee) {
  return packingSlip(amount, factor) + fee;
}

function clampPackingSlip(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  packingSlip,
  packingSlipWithFee,
  clampPackingSlip,
  factorFromBps,
};
