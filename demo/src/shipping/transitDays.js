// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 1;
const FACTOR_BASE = 136;

function transitDays(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function transitDaysWithFee(amount, factor, fee) {
  return transitDays(amount, factor) + fee;
}

function clampTransitDays(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  transitDays,
  transitDaysWithFee,
  clampTransitDays,
  factorFromBps,
};
