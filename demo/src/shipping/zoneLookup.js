// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 120;

function zoneLookup(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function zoneLookupWithFee(amount, factor, fee) {
  return zoneLookup(amount, factor) + fee;
}

function clampZoneLookup(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  zoneLookup,
  zoneLookupWithFee,
  clampZoneLookup,
  factorFromBps,
};
