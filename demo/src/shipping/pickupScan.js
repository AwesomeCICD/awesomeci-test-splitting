// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 6;
const FACTOR_BASE = 125;

function pickupScan(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function pickupScanWithFee(amount, factor, fee) {
  return pickupScan(amount, factor) + fee;
}

function clampPickupScan(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  pickupScan,
  pickupScanWithFee,
  clampPickupScan,
  factorFromBps,
};
