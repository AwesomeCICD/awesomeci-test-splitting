// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 5;
const FACTOR_BASE = 111;

function shipNotify(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function shipNotifyWithFee(amount, factor, fee) {
  return shipNotify(amount, factor) + fee;
}

function clampShipNotify(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  shipNotify,
  shipNotifyWithFee,
  clampShipNotify,
  factorFromBps,
};
