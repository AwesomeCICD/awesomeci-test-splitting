// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 100;

function loyaltyBoost(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function loyaltyBoostWithFee(amount, factor, fee) {
  return loyaltyBoost(amount, factor) + fee;
}

function clampLoyaltyBoost(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  loyaltyBoost,
  loyaltyBoostWithFee,
  clampLoyaltyBoost,
  factorFromBps,
};
