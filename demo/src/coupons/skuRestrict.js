// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 9;
const FACTOR_BASE = 118;

function skuRestrict(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function skuRestrictWithFee(amount, factor, fee) {
  return skuRestrict(amount, factor) + fee;
}

function clampSkuRestrict(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  skuRestrict,
  skuRestrictWithFee,
  clampSkuRestrict,
  factorFromBps,
};
