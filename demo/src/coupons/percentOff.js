// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 8;
const FACTOR_BASE = 139;

function percentOff(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function percentOffWithFee(amount, factor, fee) {
  return percentOff(amount, factor) + fee;
}

function clampPercentOff(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  percentOff,
  percentOffWithFee,
  clampPercentOff,
  factorFromBps,
};
