// Generated by demo/generate.mjs — do not edit by hand.
"use strict";

const UNIT = 2;
const FACTOR_BASE = 137;

function couponLocale(amount, factor) {
  return Math.round((amount * factor + UNIT) * 100) / 100;
}

function couponLocaleWithFee(amount, factor, fee) {
  return couponLocale(amount, factor) + fee;
}

function clampCouponLocale(value, min, max) {
  return Math.min(max, Math.max(min, value));
}

function factorFromBps(bps) {
  return (FACTOR_BASE + bps) / 100;
}

module.exports = {
  UNIT,
  FACTOR_BASE,
  couponLocale,
  couponLocaleWithFee,
  clampCouponLocale,
  factorFromBps,
};
